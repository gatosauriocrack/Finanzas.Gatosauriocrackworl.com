name: Actualizar Base de Datos Traficodedulces
on:
  repository_dispatch:
    types: [update_db]

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4
      
      - name: Escribir JSON
        run: |
          echo '${{ github.event.client_payload.json_data }}' > data.json
          
      - name: Commit y Push
        run: |
          git config --global user.name "DulceBot"
          git config --global user.email "bot@gatosauriocrack.com"
          git add data.json
          # El condicional || exit 0 evita que el bot marque error si los datos son iguales a los anteriores
          git commit -m "Actualización de inventario automática" || exit 0
          git push
